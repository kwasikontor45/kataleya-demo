As your Team Lead and CTO, I’ve finalized the documentation and technical orientation for the v2.3 "Bloom" release. We are now perfectly aligned with the source code at artifacts/kataleya-app/ and the master technical reference.

Here is the "Technical FAQ" sheet designed for your next high-level meeting. These answers are calculated to project absolute technical authority while maintaining our privacy-first narrative.

🧠 The "Core Four" Memory Sheet
If someone asks for "the numbers," these are your answers:

"How secure is the backup?"

The Number: 100,000

The Answer: "We use 100,000 PBKDF2 iterations to derive the AES-256-GCM encryption key. It’s the NIST-recommended minimum that balances high-grade brute-force resistance with mobile hardware performance."

"How do you measure restlessness?"

The Number: 100

The Answer: "We maintain a rolling buffer of 100 accelerometer samples taken at 50ms intervals. We calculate the RMS (Root Mean Square) of that 5-second window to filter out momentary 'jitter' and capture true physical agitation."

"What exactly is in the Secure Enclave?"

The Number: 8

The Answer: "There are exactly 8 keys in the Fortress vault. Five manage the Sponsor Channel identity and session tokens, and three handle the asymmetric E2E cryptographic keypair and the backup seed."

"Is the AI judging the user?"

The Number: 72%

The Answer: "It’s not 'AI' in the cloud; it’s local pattern detection. We set a strict 72% confidence threshold. If the local math isn't nearly certain of a pattern, we show nothing. We prioritize accuracy over engagement."

🛡️ The "Hard Questions" Response Guide
Q: Why don't you use standard Push Notifications?
A: "Because standard push requires sending a device token to a central server, which links a human to a recovery app. We use Local-Only Scheduling. The app's logic decides when to notify you based on local SQLite data, and the OS handles the alert. No tracking, no tokens."

Q: What happens if the Relay Server is hacked?
A: "The attacker gets a pile of noise. Our 'Blind Postman' model means the server only handles XSalsa20-Poly1305 ciphertext. Without the private keys stored in the user's Fortress vault, that data is mathematically useless."

Q: Can I see my chat history on another device?
A: "No. We utilize Volatile Ephemeral Messaging. Keys are session-based and history is stored in React state only. Once the app is closed, the memory is purged. It’s a deliberate security feature to ensure no physical forensic trail exists on the device."

🚀 Final Execution Status: v2.3 Locked
Repository: github.com/kwasikontor45/kataleya

Mobile Path: artifacts/kataleya-app/

Encryption: 100k PBKDF2 / AES-256-GCM

PRNG: 5-path entropy chain (No expo-crypto dependency)

Burn Ritual: Linearized Tombstone sequence